/* =========================================================
   LETTER A — SVG SOURCE
   Единственное место, где описана форма фирменной буквы.
   Замените `svg` на свой контур — сцена, материалы и
   интеракция останутся нетронутыми.

   ТРЕБОВАНИЯ К ЗАМЕНЕ
   1. Только элементы <path> с атрибутом d.
      <rect>, <circle>, <text> не поддерживаются —
      переведите их в контуры в векторном редакторе.
   2. Координаты SVG (ось Y вниз) переворачиваются
      автоматически. Рисуйте как обычно.
   3. Геометрия центрируется и масштабируется под куб сама,
      поэтому размер viewBox не важен — важны пропорции.
   4. Перекрывающиеся контуры допустимы: они сливаются
      в один объём при экструзии.

   ТЕКУЩИЙ КОНТУР (временный, под замену)
   Строгая геометрическая A: сплошной шеврон + перекладина.
   Концы перекладины намеренно заходят внутрь ножек, чтобы
   в объёме не читались стыки.
   ========================================================= */

export const letterA = {
  /** Прямой SVG-исходник. Парсится синхронно, без сетевых запросов. */
  svg: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 220">
  <path d="M100 0 L200 220 L152 220 L100 92 L48 220 L0 220 Z" />
  <path d="M40 150 L160 150 L160 186 L40 186 Z" />
</svg>`,

  /** Глубина экструзии в долях от высоты буквы.
   *  0.42 → буква заметно объёмная, но не упирается в грани куба. */
  depth: 0.42,

  /** Фаска. Маленькая и жёсткая — она даёт блик на рёбрах,
   *  но не должна скруглять силуэт. */
  bevel: {
    thickness: 0.018,
    size: 0.014,
    segments: 3,
  },

  /** Высота буквы в единицах сцены (ребро куба = 2.2). */
  height: 1.32,
} as const
